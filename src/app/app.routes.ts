import { Routes } from '@angular/router';
import { KeyboardViewComponent } from './components/keyboard-view/keyboard-view.component';

export const routes: Routes = [
  { path: '', component: KeyboardViewComponent, title: 'VIM Keyboard' },
];
